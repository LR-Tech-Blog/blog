---
import PostTagBadge from './PostTagBadge.astro';
interface Props {
    tags: string[]
}

const { tags } = Astro.props
---

<div class="flex flex-row flex-wrap gap-2 justify-start items-center">
    {
        tags.map(f => <PostTagBadge tag={f}/>)
    }
</div>