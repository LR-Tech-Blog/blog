---
import PostTagBadge from './PostTagBadge.astro';
interface Props {
    tags: string[]
}

const { tags } = Astro.props
---

<ul role="list">
    {
        tags.map(f => <PostTagBadge tag={f}/>)
    }
</ul>