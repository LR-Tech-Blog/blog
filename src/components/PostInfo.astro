---
import ReadTime from "./ReadTime.astro";

interface Props {
  author: string;
  date: string;
  minutesRead: string;
}

const { author, date, minutesRead } = Astro.props;
---

<small
  class="flex text-muted-foreground flex-row items-center align-middle gap-1"
>
  <span>by {author}</span>
  <span>●</span>
  <span>{date}</span>
  <span>●</span>
  <ReadTime readTime={minutesRead} />
</small>
